name: GitHub Actions Commit
on: push

jobs:
  say_hello:
    name: Print a hello world in every push!
    runs-on: ubuntu-latest
    steps:
    - run: |
        echo "Hello from GitHub  Actions @carlogilmar!"
